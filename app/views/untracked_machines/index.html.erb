<div id="untracked-machines-index">

<div class="row-fluid">
  <div class="span12">
    <div class="navbar">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="brand" href="<%= untracked_machines_path %>">Untracked Machines</a>
        </div>
      </div>
    </div>
  </div>
</div>

<div class="row-fluid">
  <div class="span4">
    <div class="alert alert-info">
      The following nodes have been found in the PuppetDB instances but
      are not created as a machine.
    </div>

    <div class="alert alert-warning">
      Do <strong>not</strong> blindly add those nodes as a machine. It might
      be an old entry in PuppetDB or the node has a machine with a different
      name.
    </div>
  </div>

  <div class="span8">
    <table class="table table-striped table-condensed">
      <thead>
        <tr>
          <th>FQDN</th>
          <th>Action</th>
        </tr>
      </thead>
      <tbody>
        <%- @untracked_machines.each do |fqdn| -%>
          <tr>
            <td><%= fqdn %></td>
            <td><%= link_to "Add to IDB", new_machine_path(:machine => { fqdn: fqdn }), class: "btn btn-block" %>
            <%= link_to '<i class="fa fa-times"></i> Delete'.html_safe, untracked_machine_path(fqdn), method: 'delete', class: 'btn btn-block', data: {confirm: "Really delete?"} %></td>
          </tr>
        <%- end -%>
      </tbody>
    </table>
  </div>
</div>

</div>
